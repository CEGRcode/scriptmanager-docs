### File input (GFF)

This script processes [GFF-type files][gff-format] so make sure your input is properly formatted and uses the appropriate `.gff`/`.gtf` extension if you are working from the GUI.
