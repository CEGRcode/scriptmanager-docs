### File input (FASTA)

This script processes [FASTA-type files][fasta-format] so make sure your input is properly formatted and uses the appropriate `.fa`/`.fasta`/`.fsa` extension if you are working from the GUI.
